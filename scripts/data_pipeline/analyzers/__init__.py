"""Analyzers package: response and state estimators shared with the experiments.

Submodules are imported directly (e.g. ``from analyzers.event_response import
...``); this package intentionally re-exports nothing.
"""
